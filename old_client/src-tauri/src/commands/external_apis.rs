// use tauri::{AppHandle, http::Request};

// use crate::{state::ServiceAccess, types::Config};

// #[tauri::command]
// #[specta::specta]
// pub async fn get_mods(app_handle: AppHandle) -> Result<Any, String> {
//     let req = Request::new();
//     req.
// }
